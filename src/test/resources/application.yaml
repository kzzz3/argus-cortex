argus:
  auth:
    jwt:
      secret: test-jwt-secret-for-integration-only-1234567890
  persistence:
    mode: memory

spring:
  flyway:
    enabled: true
  datasource:
    url: jdbc:h2:mem:argus_cortex;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
